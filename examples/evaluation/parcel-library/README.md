# Parcel queue

A small Python library with no third-party runtime dependencies.

Completed parcels can be processed again by calling `complete` repeatedly.

Run `make check` for the test suite. The normative contract is [here](docs/contract.md).
